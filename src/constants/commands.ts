/**
 * Constant which contains every single command
 * the user can invoke.
 */
const COMMANDS = {
  createProfileCommand: "!lk-create-profile",
  getProfileCommand: "!lk-profile",
  // Gamble commands
  rollDice: "!lk-roll-dice",
};

export default COMMANDS;
